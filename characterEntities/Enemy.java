package characterEntities;
public class Enemy extends Entity {
	
	public Enemy (String name, int maxHealth, int maxDamage, int minDamage) {
		super(name, maxHealth, maxDamage, minDamage, 200, 200);
	}

}
